spring:
  application:
    name: event

  datasource:
    url: jdbc:h2:mem:testdb
    username: admin
    password: admin
    driver-class-name: org.h2.Driver
    h2:
      console:
        enabled: true
        path: /h2-console


#  datasource:
#    url: jdbc:postgresql://localhost:5431/event_db
#    username: postgres
#    password: postgres
#    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration


